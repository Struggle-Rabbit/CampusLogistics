schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits opencode/deepseek-v4-flash-free
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# CampusLogistics

context: |
  # 项目：智慧校园系统

  ## 技术栈
  - 后端：Go 1.26+, Gin Web 框架, GORM ORM
  - 日志：zap
  - 配置：viper 
  - 数据库：SQLite (开发) / MySQL (生产)
  - 认证：JWT 认证, token和刷新Token
  - 密码: bcrypt (`golang.org/x/crypto/bcrypt`)
  

  ## 架构与约束
  - 模块路径: `github.com/Struggle-Rabbit/CampusLogistics`
  - 遵循领域驱动设计（DDD）
  - Api遵循RESTful规范（只使用POST与GET）
  - 环境切换: `ENV` 环境变量控制加载 `configs/config-dev.yaml`（默认）或 `configs/config-prod.yaml`。也可通过 `APP_` 前缀环境变量覆盖（`.` 转 `_`，如 `APP_JWT_SECRET`）。
  - 数据库: 开发模式使用 SQLite + 启动时自动迁移 13 个模型；生产模式使用 MySQL（手动迁移）。

rules:
  proposal:
    - "量化变更对现有系统的影响范围"
    - "必须包含一个可验证的成功标准"
  tasks:
    - "将任务拆分为可独立验证的步骤"
  specs:
    - "所有API接口定义必须包含完整的请求和响应示例"